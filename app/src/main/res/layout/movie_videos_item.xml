<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/movie_videos_item_layout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center|start"
    android:orientation="horizontal"
    android:paddingBottom="6dp"
    tools:ignore="UseCompoundDrawables">

    <!-- We purposely do not want to replace with a single TextView that also shows -->
    <!-- a drawable.  Picasso needs an ImageView to set the video thumbnail image. -->
    <ImageView
        android:id="@+id/ivVideoThumbnail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:adjustViewBounds="true"
        android:contentDescription="@string/video_thumbnail_image"/>

    <TextView
        android:id="@+id/tvVideoName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAlignment="viewStart"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"/>

</LinearLayout>